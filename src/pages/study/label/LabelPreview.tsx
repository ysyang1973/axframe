import * as React from "react";
import styled from "@emotion/styled";

interface Props {}

export function LabelPreview({}: Props) {
  return <Container>LabelPreview</Container>;
}

const Container = styled.div``;
