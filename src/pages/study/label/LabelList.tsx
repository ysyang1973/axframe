import * as React from "react";
import styled from "@emotion/styled";

interface Props {}

export function LabelList({}: Props) {
  return <Container>LabelList</Container>;
}

const Container = styled.div``;
